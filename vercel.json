{
  "version": 2,
  "buildCommand": "cd b2b-code && npm install && npm run build",
  "outputDirectory": "b2b-code/dist",
  "builds": [
    {
      "src": "b2b-code/src/api/**/*.ts",
      "use": "@vercel/node"
    },
    {
      "src": "b2b-code/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "b2b-code/src/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "VITE_API_URL": "https://${VERCEL_URL}"
  }
}
